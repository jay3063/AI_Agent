# SW 상세설계서 템플릿 (TPL-SWE3-001 동기화본)

> **이 파일은 PLACEHOLDER가 아니다.** 실제 공식 템플릿은
> `WP_Templates/Engineering/SoftwareDetailedDesignAndUnitConstruction/TPL-SWE3-001_SW 상세설계서 템플릿.docx`
> 이며, 이 마크다운은 그 문서의 목차와 섹션별 작성 안내를 그대로 옮긴 작업용 사본이다.
> 산출물은 **반드시 아래 순서와 제목을 그대로 따른다.** 원본 docx가 개정되면 이 파일도 함께 갱신한다.
>
> UML/호출관계 다이어그램은 `TPL-SWE3-002_상세설계 UML 및 호출관계 템플릿.drawio`의 표기 규칙(구현 단위와 책임, 함수/클래스 호출, 입출력 자료형, 설계·시험 추적 ID)을 따르되, 산출물 본문에는 Mermaid 코드 블록으로 동일 내용을 표현한다(리뷰 가능성 확보).

## 문서 통제

- 템플릿 ID: TPL-SWE3-001 / 적용 프로세스: SWE.3
- 문서 통제표(작성 문서 ID/명칭, 버전 및 베이스라인, 작성자/검토자), 변경 이력, 작성·검토·승인 상태 표를 문서 서두에 포함한다.

## 목차 및 섹션별 작성 안내

1. **목적 및 적용범위**
   1.1 목적 — 문서가 지원하는 개발/검증 활동과 기대 결과
   1.2 적용범위 — 대상 제품, SW 구성요소, 생명주기 단계, 대상 조직
   1.3 적용 경계 — 포함/제외 범위, 외부 조직·상위 산출물(아키텍처 설계서)과의 경계
2. **모듈 분해** — 아키텍처 요소를 구현 단위(모듈/클래스/파일)로 분해, 각 단위의 ID·책임·소스 위치
3. **상세 호출관계** — 함수/클래스 간 호출 순서, 의존 방향, 주요 데이터 흐름
4. **공통 자료형** — 공통 데이터 구조, 열거형, 단위, 범위, 불변조건, 직렬화 규칙
5. **핵심 함수 계약** — 함수별 입력/출력/사전조건/사후조건/부작용/예외/시간 제약 (`references/function-contracts-and-build-order.md` 형식 사용)
6. **핵심 알고리즘** — 처리 순서, 판단 조건, 경계값, 계산 근거를 의사코드 또는 흐름도로 작성
7. **정책 의사결정표** — 입력 조건 조합, 우선순위, 기대 동작, 충돌 해결 규칙
8. **상태전이 상세** — 상태 저장 위치, 전이 함수, 이벤트, 가드, 타이머, 초기화 동작
9. **Web 및 API 상세** — 엔드포인트, 요청/응답 구조, 검증, 오류 코드, 세션, 보안 경계 (해당 시)
10. **오류와 방어 동작** — 유효하지 않은 입력/예외/자원 실패의 검출, 처리, 기록, 복구
11. **코딩 및 검증 규칙** — 코딩 표준, 정적분석, 단위검증, 커버리지, 리뷰 기준
12. **단위와 요구사항 할당** — 구현 단위를 아키텍처 요소, SW 요구사항, 단위시험에 연결
13. **구현 경계** — 생성 코드, 외부 라이브러리(FOSS 포함), 플랫폼 종속부, 구현하지 않는 범위
14. **추적성** — 아키텍처 ↔ 상세설계 ↔ 소스 파일/함수 ↔ 단위시험 양방향 연결
15. **참고자료** — 아키텍처 설계서, 코딩규칙, API 문서, 외부 라이브러리 자료

## 이 스킬이 목차에 추가하는 산출물

- **설계 대안 검토 기록** — 알고리즘/자료구조 선택에 의미 있는 대안이 있는 단위에 대해서만, 섹션 6(핵심 알고리즘) 앞에 첨부. 대안 비교, 선택 근거, 결정자 기록.
- **SOLID / 응집도·결합도 자체 점검표** — 섹션 2(모듈 분해)와 5(핵심 함수 계약) 뒤에 첨부.
- **단위 구현·통합 순서 표** — 섹션 12 뒤에 첨부 (`references/function-contracts-and-build-order.md` 3절).
- **A-SPICE SWE.3 / ISO 26262-6 상세설계·구현 원칙 자체 점검 결과** — 문서 최종 섹션에 별도 추가.
